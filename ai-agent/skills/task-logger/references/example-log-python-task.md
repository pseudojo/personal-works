# TASKLOG-YYYYY

**Task:** TASK-0002.1 - Create a Python script to print a greeting

**Timestamp:** 2026-02-03T10:00:00Z
**Date:** 2026-02-03
**Action:**
Created a Python script `hello.py` that takes a name as a command-line argument and prints a "Hello, [name]!" message.

**Details:**
- The script uses the `argparse` module to handle command-line arguments.
- A new file `hello.py` was created.
- This script serves as a basic example of a command-line tool.

**Command:**
```shell
python3 hello.py --name "World"
```

**Output:**
```
Hello, World!
```